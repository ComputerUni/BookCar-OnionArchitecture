@using X.PagedList
@using X.PagedList.Mvc.Core
@model IPagedList<ResultCategoryDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = (Model.PageNumber - 1) * Model.PageSize;
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">

            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <div class="mt-3">
                                <h4 class="card-title">Blog Kategori Listesi</h4>
                                <p class="card-subtitle mb-4"><code class="text-info font-weight-bold">Kategorilerle ilgili ekleme, silme, arama ve listeleme gibi işlemleri bu sayfadan yapabilirsiniz.</code></p>
                            </div>

                            <div class="mt-3">
                                <a href="/Admin/AdminCategory/CreateCategory" class="btn btn-outline-info"><i class="fa fa-plus"></i> Yeni Kategori</a>
                            </div>
                        </div>
                        <div class="table-responsive">

                            <table class="table table-hover mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Kategori Adı</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <th scope="row">@count</th>
                                            <td>@item.Name</td>
                                            <td d-flex mr-3>
                                                <a href="/Admin/AdminCategory/RemoveCategory/@item.CategoryId" class="btn btn-outline-danger"><i class="fa fa-trash"></i></a>
                                                <a href="/Admin/AdminCategory/UpdateCategory/@item.CategoryId" class="btn btn-outline-secondary"><i class="fa fa-pencil-alt"></i></a>
                                                <a href="/Admin/AdminCategory/#/@item.CategoryId" class="btn btn-outline-info"><i class="fa fa-eye"></i></a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        <div class="d-flex justify-content-left mt-4">
                            @Html.PagedListPager(Model, page => Url.Action("Index", new { page }), new PagedListRenderOptions
                            {
                                LiElementClasses = new string[] { "page-item" },
                                PageClasses = new string[] { "page-link" },
                                DisplayLinkToFirstPage = PagedListDisplayMode.Always,
                                DisplayLinkToLastPage = PagedListDisplayMode.Always,
                                DisplayLinkToPreviousPage = PagedListDisplayMode.Always,
                                DisplayLinkToNextPage = PagedListDisplayMode.Always
                            })
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
